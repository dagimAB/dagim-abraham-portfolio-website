# Example environment variables for contact/email
# Provide real values in Netlify/Vercel or local .env when configuring SMTP or SendGrid

# If using SMTP (nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# If using SendGrid
SENDGRID_API_KEY=your_sendgrid_api_key
CONTACT_TO=your@example.com
